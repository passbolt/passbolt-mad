<li id="{{mappedItem.id}}" class="{{mappedItem.cssClasses}}{{#unless(mappedItem.enabled}} disabled{{/unless}}">
	<div class="row">
		<div class="main-cell-wrapper">
			<div class="main-cell">
				<a><span>{{mappedItem.label}}</span></a>
			</div>
		</div>
	</div>
	
{{#if(hasChildren)}}
	<ul>
	</ul>
{{/if}}

</li>
